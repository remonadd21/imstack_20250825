# 복습문제

Q1. form 태그에서 요청할 때 , 눈 여겨봐야 할 부부은 (처리 컨테이너, 처리방식 , 보관용기) 이다.
Q2. 넘겨받은 값을 확인 할때는 ( request.getParameter("name 값"))
Q3. jdbc 연동할때 ojdbc6.jar 파일의 위치는?()
> webadd -> web-inf-> lib

    jdbc 연동할때 순서는? (드크프리)
        ### 드라이버 이름
        Class.forName(드라이버이름)
        ### url, id, pass
        Connection conn = 

        DriverManager
        Connection
        PreparedStament
        ResultSet


> 다음과 같은 테이블이 있다.
sql> desc dept

name        null?           type
deptno      not null        number
dname                       varchar2(14)
loc                         varchar2(13)

Q5) insert(필드 넣는 방식과 안넣는 방식)
위의 테이블에 (50, 'database', 'incheon'), (60, 'ai', 'incheon')

넣는 방식) insert into dept (deptno, dname, loc) values(50, 'database', 'incheon'),

안넣는 방식) insert into deptp values (60, 'ai', 'incheon')


Q6) update 번호가 60인 데이터의 loc를 busan으로 변경하기
update dept set loc='busan' where deptno =60;

Q7) delete 50번 이상의 데이터를 삭제하시오.
delete from deptno where detpno=>50;
Q8) 위의 내용을 반영하려면?
commit

------------------------------------------------------------------------------------

project001

1. dynamic web project 만들기 : project001
2. ojdbc6.jar 파일 셋팅
3. mvc1 + preparedStatement
    [member] - 회원가입 (join.jsp), 로그인 (login.jsp)
    회원가입폼      회원가입처리
    join.jsp -> join_process.jsp

4. 로그인폼             로그인처리
    login.jsp   ->      login_process.jsp

----------------------------------------------------------------------------------------

# join 부터 login 까지 실습
# 중복위험이 없도록 시퀀스 배우기
